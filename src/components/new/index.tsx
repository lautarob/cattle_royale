import styled from 'styled-components';

const Container = styled.div`
  text-align: center;
  padding: 30px;
`

const StartNew = () => (
  <Container>
    Start New Matchup
  </Container>
);

export default StartNew;